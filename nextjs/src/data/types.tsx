export type Country = {
    id: number;
    name: string;
    phone_code: string;
    phone_digits_number: number;
    currency_code: string;
};

export type Product = {
    id: number;  
    name: string;  
    price: number; 
};

